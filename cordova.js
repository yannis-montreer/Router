// Cordova shim for PWA hosting — this file is overwritten by the Cordova
// build process (cordova run android) with the real cordova.js bridge.
// When served as a PWA (GitHub Pages / Netlify), this empty shim prevents
// a 404 and window.cordova remains undefined, which is the correct signal
// for the app to follow the PWA code path.
